[pytest]
# The appliance's suite lives in tests/ only. Without this, a bare `pytest`
# also collects experiments/ (different deps, e.g. openpyxl) and any stray
# checkout, and dies at collection before running a single real test.
testpaths = tests
